pre-commit:
  parallel: true
  jobs:
    - name: lint
      root: "client/"
      glob: "client/src/**/*.{ts,tsx}"
      run: bunx biome check {staged_files}

    - name: typecheck
      root: "client/"
      glob: "client/src/**/*.{ts,tsx}"
      run: bun run build
